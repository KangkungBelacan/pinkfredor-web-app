import { GenericProps } from "../../../interface/GenericProps";

const UserGeneralSettings = (props: GenericProps) => {
    return (
        <div {...props}>
            General Settings
        </div>
    );
};

export default UserGeneralSettings;